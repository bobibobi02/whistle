model User {
  id                    String   @id @default(cuid())
  email                 String   @unique
  // ... existing fields ...
  encryptionPublicKey   String?  // base64-encoded public key
  encryptionPrivateKey  String?  // base64-encoded encrypted private key (encrypted at rest)
}
